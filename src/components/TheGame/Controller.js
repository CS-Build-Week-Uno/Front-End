import React from "react";

import styled from "styled-components";

const Controller = () => {
  return null;
  //in here we will put the WSAD or up down left right buttons
};

export default Controller;
