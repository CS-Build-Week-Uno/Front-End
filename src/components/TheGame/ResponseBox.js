import React from "react";

import styled from "styled-components";

const ResponseBox = () => {
  return null;
  //in here we will put a log that shows the game options and responses
};

export default ResponseBox;
